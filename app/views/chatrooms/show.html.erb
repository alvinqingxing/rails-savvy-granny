  <div class="container">
  <div class="row">
    <div class="col-12 col-lg-4 chatroom-sidebar">
      <div class="mb-3">
        <a class="text-primary d-flex justify-content-between" data-toggle="collapse" data-target="#sidebar-one" aria-expanded="true" aria-controls="sidebar-one">
          <div>Booking Information</div>
          <div><i class="fas fa-caret-down"></i></div>
        </a>
        <div class="collapse show" id="sidebar-one">
          <div class="card card-body p-0 border-0">
            Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica, craft beer labore wes anderson cred nesciunt sapiente ea proident.
          </div>
        </div>
      </div>

      <div class="mb-3">
        <a class="text-primary d-flex justify-content-between" data-toggle="collapse" data-target="#sidebar-two" aria-expanded="false" aria-controls="sidebar-two">
          <div>Videocall Message</div>
          <div><i class="fas fa-caret-down"></i></div>
        </a>
        <div class="collapse" id="sidebar-two">
          <div class="card card-body p-0 border-0">
            <div>Copy & paste the following message to your parent:</div>
            <% if @chatroom.booking.id <= 30 %>
            <% videolink = "https://savvygranny.daily.co/#{@chatroom.booking.id}" %>
            <% else %>
            <% videolink = "https://savvygranny.daily.co/#{@chatroom.booking.id - 30}" %>
            <% end %>
            <textarea class="mt-2 mb-3 form-control input-lg" type="text" rows="5" id="invitation">Hey, remember that tech issue you had with <%= @chatroom.booking.job.name %>? I've arranged for a savvy tutor to help you with it! :D Just click this link: <%= videolink %> on <%= @chatroom.booking.start_date.strftime("%d %b") %> at <%= @chatroom.booking.start_time.strftime("%I:%M%p") %>!</textarea>
            <div class="input-group">
              <button id="copy" class="form-control btn btn-outline-primary fix-font"><i class="far fa-copy"></i> Copy</button>
              <a href="https://wa.me/?text=paste%20the%20invitation%20here" class="form-control btn btn-outline-primary" role="button"><i class="fab fa-whatsapp" aria-hidden="true"></i> WhatsApp</a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-12 col-lg-8">
      <div class="chatroom-chatbox">
      chatbox is in here
      </div>
    </div> 
  </div>

    <div class="col-6 offset-3 py-5">
      <div class="border-bottom mb-3">
        <h1>CHATROOM #<%= @chatroom.booking.id %></h1>
        <%# videocall daily.co link %>
        <h6> Your assigned tutor is: <i class="fa fa-user-circle-o" aria-hidden="true"></i> <%= link_to "#{@chatroom.booking.tutor.first_name} #{@chatroom.booking.tutor.last_name}", @chatroom.booking.tutor %>

          <br><br> Just copy & paste the following invitation to your parent: </h6>
          <% if @chatroom.booking.id <= 30 %>
          <% videolink = "https://savvygranny.daily.co/#{@chatroom.booking.id}" %>
          <% else %>
          <% videolink = "https://savvygranny.daily.co/#{@chatroom.booking.id - 30}" %>
          <% end %>
          <textarea class="form-control input-lg" type="text" rows="5" id="invitation">Hey, remember that tech issue you had with <%= @chatroom.booking.job.name %>? I've arranged for a savvy tutor to help you with it! :)

You just have to click this link: <%= videolink %> on <%= @chatroom.booking.start_date.strftime("%d %b") %> at <%= @chatroom.booking.start_time.strftime("%I:%M%p") %>!</textarea>
          <br>
          <div class="input-group">
            <button id="copy" class="form-control btn btn-outline-dark fix-font"><i class="far fa-copy"></i> Copy Invitation</button>
            <a href="https://wa.me/?text=paste%20the%20invitation%20here" class="form-control btn btn-outline-dark" role="button"><i class="fab fa-whatsapp" style="color: #25D366" aria-hidden="true"></i> Send Whatsapp</a>
          </div>
          <%# end of videocall link %>
          <br>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="row justify-content-center" style = "padding-bottom: 100px">
        <div class="col" style = "max-width: fit-content">
          <div class="chat-header"><h4>#<%= @chatroom.booking.id %></h4></div>
          <div class="messages">
            <% @messages.each do |message| %>
            <% if current_user.id == message.sender_id%>
            <%# right msg from sender %>
             <% if message.content %>
                <div class="message right">
                <%= message.sender.first_name%> <%= message.created_at.strftime("%a %b %e at %l:%M%p")%>
                <% if message.read %>
                  <i class="far fa-eye"></i>
                <% else %>
                  <i class="far fa-eye-slash"></i>
                <% end %>
              <div class="message-content">
                <%= message.content %>
              </div>
            </div>
            <% end %>
            
            <%# left msg from receiver %>
            <% else %>
             <% if message.content %>
                <div class="message">
                <%= message.sender.first_name%> <%= message.created_at.strftime("%a %b %e at %l:%M%p")%>
                <% if message.read %>
                    <i class="far fa-eye"></i>
                <% else %>
                  <i class="far fa-eye-slash"></i>
                <% end %>
                <div class="message-content">
                  <%= message.content %>
                </div>
              </div>
              <% end %>
              <% end %>
            <% end %>
          <%# </div> %>
          <div id="create-message">
            <%= simple_form_for [ @chatroom, @message ], remote: true, html: {autocomplete: "off" } do |f| %>
            <%= f.input :content, label: false %>
            <%= f.button :submit, "SEND", class: 'btn btn-primary btn-block' %>
            <% end %>
          </div>
          </div>
        </div>
      </div>
    <%# </div> %>
    <%# <% content_for :after_js do %>

    <%# <% end %>
